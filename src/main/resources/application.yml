
# DB config
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/tutorlink
    username : root
    password : 1234
  # JPA config
  jpa:
    #database: mysql
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql
    properties:
      hibernate:
        '[format_sql]': true
        show_sql: true
    open-in-view: false
